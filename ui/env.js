// This file is used to set the environment variables for the application.
// key value pairs are set in the window._env_ object.
// the values are replaced by the actual values during the build process.
/*
  Example:

  window._env_ = {
    TEMPLATE_VAR: '__TEMPLATE_VAR__',
  };

  In the above example, the value of TEMPLATE_VAR is set to '__TEMPLATE_VAR__'

  During the start process, the value of TEMPLATE_VAR is replaced by the actual value.
  You can set the value of TEMPLATE_VAR in the .env file.

  To use the value in the application, use the following code:

  const templateVar = window._env_.TEMPLATE_VAR;
*/

window._env_ = {
  TEMPLATE_VAR: '__TEMPLATE_VAR__',
};
